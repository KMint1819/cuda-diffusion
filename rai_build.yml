rai:
  version: 0.2
  image: kmint1819/gten
resources:
  cpu:
    architecture: amd64
  gpu:
    count: 1
  network: false
  cache: false
commands:
  build:
    - ls
    - pwd
    - nsys profile --trace=cuda,cudnn,cublas,osrt,nvtx --delay=60 python3.8 /src/python/play.py
    # - nvprof --print-gpu-trace -o profile python3.8 /src/python/play.py
    # - cmake /src
    # - make
    # - nvprof --cpu-profiling on --export-profile timeline.nvprof -- ./gten ko-i /src/python/play.py -o output
    # - ./gten -a
