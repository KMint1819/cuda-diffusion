rai:
  version: 0.2
  image: kmint1819/gten
resources:
  cpu:
    architecture: amd64
  gpu:
    count: 1
  network: false
  cache: false
commands:
  build:
    - python3.8 --version
    # - cmake /src
    # - make
    # - ./gten -a
