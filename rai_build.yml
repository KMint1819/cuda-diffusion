rai:
  version: 0.2
  image: kmint1819/gten:local
resources:
  cpu:
    architecture: amd64
  gpu:
    count: 1
  network: false
  cache: false
commands:
  build:
    - ls
    - pwd
    - nsys profile --trace=cuda,cudnn,cublas,osrt,nvtx python3.8 /src/python/forward_old_attention.py
    # - cmake /src
    # - ./gten -a
